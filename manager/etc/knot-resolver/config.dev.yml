id: dev
rundir: etc/knot-resolver/runtime
server:
  workers: 1
  management:
    interface: 127.0.0.1@5000
cache:
  storage: ../cache
logging:
  level: debug
  groups:
    - manager
network:
  listen:
    - interface: 127.0.0.1@5353

cache:
  storage: ../cache
logging:
  level: debug
  groups:
    - manager
network:
  listen:
    - ip-address: 127.0.0.1@5353
server:
  workers: 1
  rundir: etc/knot-resolver/runtime
  management:
    listen:
      ip: 127.0.0.1
      port: 5000
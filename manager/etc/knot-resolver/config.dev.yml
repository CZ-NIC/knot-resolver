rundir: etc/knot-resolver/runtime
workers: 1
management:
  interface: 127.0.0.1@5000
cache:
  storage: ../cache
logging:
  level: notice
  groups:
    - manager
    - supervisord
network:
  listen:
    - interface: 127.0.0.1@5353

#!/bin/sh
set -e

# upgrade script to 5.0
if [ -f /lib/systemd/system/kresd.socket ] && [ ! -e /etc/knot-resolver/upgrade5 ] then
	echo "manual upgrade required: 'touch /etc/knot-resolver/upgrade5'"
	exit 1
fi

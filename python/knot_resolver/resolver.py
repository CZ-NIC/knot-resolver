from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


async def start_resolver(config: list[str]) -> int:
    logger.info("Starting Knot Resolver...")

    return 0
